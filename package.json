{
  "name": "bv-ui-core",
  "private": true,
  "version": "0.0.0-pre-anything",
  "description": "Bazaarvoice UI-related JavaScript",
  "repository": {
    "type": "git",
    "url": "git://github.com/bazaarvoice/bv-ui-core"
  },
  "scripts": {
    "lint": "eslint lib test",
    "test": "tap test",
    "test": "tap --cov test"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^0.24.1",
    "ghooks": "^0.3.2",
    "tap": "^1.3.1"
  },
  "config": {
    "ghooks": {
      "pre-commit": "node_modules/.bin/eslint lib test",
      "pre-push": "node_modules/.bin/tap test"
    }
  }
}
